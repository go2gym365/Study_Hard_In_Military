#include<bits/stdc++.h>

using namespace std;

int n, m;
vector<int> vec;

int main() {
    cin >> n >> m;
    
    for(int i = 0; i < n; i++) {
        int a;
        cin >> a;
        vec.push_back(a);
    }

    
}