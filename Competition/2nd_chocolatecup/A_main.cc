#include<bits/stdc++.h>

using namespace std;

bool solve(string str) {
    if()
}

int main() {
    int t;
    cin >> t;

    while(t--) {
        string str;
        cin >> str;

        solve(str);
    }
}