#include<bits/stdc++.h>

using namespace std;

int main() {
    int t;
    cin >> t;

    while(t--) {
        multiset<int> ms;

        int n;
        cin >> n;

        for(int i = 0; i < n; i++) {
            char c;
            int num;

            cin >> c >> num;
        }
    }
}