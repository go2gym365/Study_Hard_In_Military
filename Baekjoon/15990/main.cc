#include<bits/stdc++.h>

using namespace std;

int T;

int dp[100001];

int solve(int n) {
    if(n == )
}

int main() {
    cin >> T;
    for(int i = 0; i < T; i ++) {
        int n;
        cin >> n;

        solve(n);
    }
}