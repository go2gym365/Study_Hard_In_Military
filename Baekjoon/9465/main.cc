#include<bits/stdc++.h>

using namespace std;

int t;

vector<int> vec;

int main() {
    cin >> t;

    while(t--) {
        int n;
        cin >> n;

        for(int i = 0; i < n; i++) {
            int a;
            cin >> a;
            vec.push_back(a);
        }

        
    }


}