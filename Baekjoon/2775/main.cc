#include<bits/stdc++.h>

using namespace std;

int dp[15];

solve(int ) {
    if()
}

int main() {
    int t;
    cin >> t;

    for(int i = 0; i < t; i++) {
        int k, n;
        cin >> k >> n;

        solve(k, n);
    }
}