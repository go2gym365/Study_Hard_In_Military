#include<bits/stdc++.h>

using namespace std;

int main() {
    int m;
    string str;
    cin >> m;

    for(int i = 0; i < m; i++) {
        string str;
        cin >> str;

        if(str == "add") {
            int a;
            cin >> a;
            
        }
    }
}