#include<bits/stdc++.h>

using namespace std;

vector<vector<int>> vec(16, vector<int>(16, 0));
vector<vector<bool>> vec(16, vector<bool>(16, 0));

int n;

void queen(int x) {
    if(n == x) ans++;
    else {
        for(int i = 0; i < n; i++) {
            
        }
    }
}

int main() {
    cin >> n;

    solve();
}